{
  "name": "ogp-generator",
  "version": "1.0.0",
  "description": "Dynamic OGP image generator for Audio Review",
  "main": "index.js",
  "scripts": {
    "deploy": "serverless deploy",
    "deploy:prod": "serverless deploy --stage prod",
    "test": "node test.js",
    "start": "node server.js",
    "test:docker": "docker-compose up --build",
    "test:docker:clean": "docker-compose down --rmi all"
  },
  "dependencies": {
    "canvas": "^2.11.2"
  },
  "devDependencies": {
    "serverless": "^3.38.0"
  }
} 